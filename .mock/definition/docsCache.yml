imports:
  commons: commons.yml
service:
  auth: true
  base-path: /docs-cache
  endpoints:
    invalidate:
      method: POST
      path: /invalidate
      request:
        name: InvalidateCachedDocsRequest
        body:
          properties:
            url: string
